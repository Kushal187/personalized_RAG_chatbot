# Required: OpenAI API key (embeddings + chat for RAG and agent)
OPENAI_API_KEY=your_openai_api_key_here

# Required for agent web search (get a key at https://tavily.com)
TAVILY_API_KEY=your_tavily_api_key_here

# Optional: GitHub API token for higher rate limits when the agent searches repos
# Without it, unauthenticated GitHub API is limited to 60 requests/hour
# GITHUB_TOKEN=your_github_personal_access_token_here
